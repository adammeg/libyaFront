import { LayoutDashboard, Car, PlusCircle, List, Users, Settings, type LucideIcon } from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  "layout-dashboard": LayoutDashboard,
  car: Car,
  "plus-circle": PlusCircle,
  list: List,
  users: Users,
  settings: Settings,
}

